function GuessLineEndings () {
}

// returns start offset
GuessLineEndings.prototype.arrayMatch = function (array, start, end, subArray) {
}

GuessLineEndings.prototype.canEndWithSplittedArray = function (array, start, end, subArray) {        
}

GuessLineEndings.prototype.getLineEnd = function (buffer, start, end, isEof) {
    return 0;
}

module.exports = GuessLineEndings;